$('#comments-area').prepend('<%= j(render 'created_comment') %>');

$('#comment-errors').html('');
$('#no-comments').html('');
$('.comment-text-form').val('');

$(function() {
  window.globalFunction.link();
});